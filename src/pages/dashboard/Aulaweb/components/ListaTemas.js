// @flow

import React from 'react';
type ListaTemasProps = {
    value: number,
    onClick: (date: any) => void,
    data: Array<any>,
};

const ListaTemas = (props: ListaTemasProps): React$Element<any> => {
    return <React.Fragment></React.Fragment>;
};

export default ListaTemas;
