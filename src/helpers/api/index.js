// @flow
import { login, logout, signup, forgotPassword, forgotPasswordConfirm } from './auth';
import { subrifoto } from './apiDocumentos';
export { login, logout, signup, forgotPassword, forgotPasswordConfirm, subrifoto };
